# components/ui

Auto-generated by the [shadcn](https://ui.shadcn.com) CLI. Do not edit these files manually.

## Adding a component

```bash
npx shadcn add <component>
```

## Rules

- Never refactor these files to match project conventions (arrow functions, default exports, etc.) — shadcn will overwrite them on the next update
- If a component needs customisation, wrap it in `src/components/` instead of modifying the file here
- Accessibility lint rules are intentionally suppressed for this folder (see `biome.json` overrides)
