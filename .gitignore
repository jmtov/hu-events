# Logs
logs
*.log
npm-debug.log*
yarn-debug.log*
yarn-error.log*
pnpm-debug.log*
lerna-debug.log*

node_modules
dist
dist-ssr
*.local

# Claude Code — versioned commands/skills; keep local overrides private
.claude/settings.local.json

# Local dev API server (not needed in production)
dev-api-server.mjs

# Environment variables — never commit secrets
.env
.env.local
.env*.local

# Editor directories and files
.vscode/*
!.vscode/extensions.json
.idea
.DS_Store
*.suo
*.ntvs*
*.njsproj
*.sln
*.sw?
.vercel

# Mock data store — local dev only, derived from fixtures
api/_fixtures/store/
